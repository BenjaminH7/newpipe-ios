# expo-router (SDK 57) pulls in optional web-only deps (vaul/@radix-ui) whose
# react-dom peer range is ahead of the react version Expo pins. Harmless for a
# mobile-only app, but npm's strict resolver blocks on it without this.
legacy-peer-deps=true
